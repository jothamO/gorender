# Backlog (Open-Source)

## P0

- Publish and enforce lightweight charter + budgets in release flow.
- Document official frontend contract (ready/frame-ready/metadata).
- Document presets and duration-source behavior with examples.
- Add migration note from `--profile` to `--preset`.

## P1

- Add architecture diagrams for render pipeline.
- Add performance tuning guide (workers, fps, frame-step, upscale).
- Add failure-mode handbook (timeouts, connection refused, chrome startup).

## P2

- Add sample integrations (vanilla first, then optional framework packs).
- Add containerized deployment docs.
- Add cloud/distributed rendering RFC.
