# Template Packs (Optional)

Goal: help developers bootstrap composition projects while keeping core framework-neutral.

## Packaging Strategy

- Built-in minimal pack: `vanilla` only.
- Optional lightweight pack: `svelte`.
- External/community packs: `react`, `next`, others.

## Principle

Templates are adapters around the render contract, not core runtime dependencies.
